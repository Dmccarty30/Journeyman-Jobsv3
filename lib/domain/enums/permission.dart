/// Defines the permissions available within the crew management system.
enum Permission {
  createCrew,
  updateCrew,
  deleteCrew,
  inviteMember,
  removeMember,
  updateRole,
  shareJob,
  moderateContent,
  viewStats,
  manageSettings,
}
